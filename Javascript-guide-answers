/*
function palindrome(str) {
  // We get a string and then we take out special characters  
  var strReverseX = str.replace(/[^a-zA-Z0-9]+/g,'');    
  // reverse the string
  var strReverse = strReverseX.split('').reverse().join('');    
  // lowercase both
  strReverseX = strReverseX.toLowerCase();
  strReverse = strReverse.toLowerCase();
  console.log( strReverseX + " == " + " " + strReverse);
  return (strReverseX === strReverse);
} 

function titleCase(str) {
  
  var words = str.split(' ');
  var upperWord = 1, lengthArray, newSentence = "", wordNew;
  
  lengthArray = words.length;
  
  
  for (var i=0; i<lengthArray;i++) {
      
    wordNew = (words[i].substr(0,1)).toUpperCase() + (words[i].substr(1,words[i].length)).toLowerCase();
    newSentence = newSentence + wordNew + " " ;
  }
  newSentence = newSentence.substr(0,str.length);
  
  return newSentence;
  //  console.log(i + " " + newSentence);
}

titleCase("HERE IS MY HANDLE HERE IS MY SPOUT"); 

function largestOfFour(arr) {
    var largeNo = 0;
    var newArray = [];
    
  for (i=0;i<arr.length;i++) {
      
      largeNo = 0;
      for (j=0;j < 4;j++) {
          
         if (largeNo < arr[i][j]) {
            largeNo = arr[i][j];
         }
         
         
      }
      newArray.push(largeNo);
      console.log(largeNo + " " + newArray);
  }
  return newArray;
  
}

largestOfFour([[4, 5, 1, 3], [13, 27, 18, 26], [32, 35, 37, 39], [1000, 1001, 857, 1]]); 

function confirmEnding(str, target) {
  
  var wordLen = target.length;
  var endStr = str.substr(str.length - wordLen, wordLen);
  return (endStr == target);
}

confirmEnding("Walking on water and developing software from a specification are easy if both are frozen", "frozen");

function repeatStringNumTimes(str, num) {
  // repeat after me
  var strX = "";
  
  for (i=1;i<num+1;i++) {
     strX = strX + str;
  }
  return strX;
}

repeatStringNumTimes("abc", 3);
*/
